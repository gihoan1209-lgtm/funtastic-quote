import './globals.css'
export const metadata = { title: '통합견적서', description: '견적서 작성 도구' }
export default function RootLayout({ children }) {
  return <html lang="ko"><body>{children}</body></html>
}
